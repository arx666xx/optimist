<Window x:Class="NetOptimizer.RebootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Перезагрузка"
        Height="240" Width="470"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBg}"
        Foreground="{DynamicResource FgBrush}"
        FontFamily="Segoe UI Variable Text, Segoe UI" FontSize="13"
        UseLayoutRounding="True">

    <Grid Margin="24,22,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" Style="{StaticResource H1}" FontSize="16" Margin="0,0,0,12"/>
        <TextBlock x:Name="MsgText" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top"
                   Style="{StaticResource Muted}"/>

        <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,16,0,0">
            <Button x:Name="BtnCancel" Content="Отмена" DockPanel.Dock="Right" Style="{StaticResource Btn}" Margin="9,0,0,0" Click="Cancel_Click"/>
            <Button x:Name="BtnReboot" Content="Перезагрузить сейчас" DockPanel.Dock="Right" Style="{StaticResource BtnAccent}" Click="Reboot_Click"/>
        </DockPanel>
    </Grid>
</Window>
