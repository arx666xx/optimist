<Window x:Class="NetOptimizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetOptimizer"
        Height="760" Width="1360"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource FgBrush}"
        FontFamily="Segoe UI" FontSize="13">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header + tabs -->
        <DockPanel Grid.Row="0" Margin="2,0,2,12" LastChildFill="False">
            <Button Content="⚙ Настройки" DockPanel.Dock="Right" Style="{StaticResource Btn}" Click="Settings_Click"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                <TextBlock Text="Net" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                <TextBlock Text="Optimizer" FontSize="20" FontWeight="Bold"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" Margin="26,0,0,0" VerticalAlignment="Center">
                <Button x:Name="BtnViewConn" Content="Сетевые соединения" Style="{StaticResource TabBtn}" Tag="active" Click="ShowConnections_Click"/>
                <Button x:Name="BtnViewLog" Content="Журнал Windows" Style="{StaticResource TabBtn}" Tag="inactive" Margin="8,0,0,0" Click="ShowLog_Click"/>
            </StackPanel>
        </DockPanel>

        <!-- ============ CONNECTIONS VIEW ============ -->
        <Grid x:Name="ConnectionsView" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PanelBrush}" CornerRadius="10"
                    BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1"
                    Padding="12,10" Margin="0,0,0,12">
                <DockPanel LastChildFill="False">
                    <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{DynamicResource MutedBrush}"/>
                    <TextBox x:Name="FilterBox" Width="220" Style="{StaticResource SearchBox}" TextChanged="FilterBox_TextChanged"/>
                    <CheckBox x:Name="HideLoopback" Content="Скрыть локальные" Margin="14,0,0,0" Click="Filter_Changed"/>
                    <CheckBox x:Name="OnlyActive" Content="Только активные" Margin="14,0,0,0" Click="Filter_Changed"/>
                    <CheckBox x:Name="OnlySuspicious" Content="Подозрительные" Margin="14,0,0,0" Click="Filter_Changed"/>
                    <CheckBox x:Name="GroupByProcess" Content="Группировать" Margin="14,0,0,0" Click="GroupBy_Changed"/>

                    <Button Content="Обновить" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource Btn}" Click="Refresh_Click"/>
                    <CheckBox x:Name="AutoRefresh" Content="Авто" DockPanel.Dock="Right" Margin="8,0,0,0" IsChecked="True" Click="AutoRefresh_Changed" VerticalAlignment="Center"/>
                    <Button x:Name="BtnUpdate" Content="Обновления" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource Btn}" Click="Update_Click"/>
                    <Button Content="👁 Столбцы" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource Btn}" Click="Columns_Click"/>
                    <Button Content="💾 Экспорт" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource Btn}" Click="Export_Click"/>
                    <Button Content="🛠 Ремонт сети" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource Btn}" Click="Repair_Click"/>
                    <Button Content="🚀 Ускорить" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource Btn}" Click="Boost_Click"/>
                </DockPanel>
            </Border>

            <DataGrid x:Name="ConnGrid" Grid.Row="1"
                      Style="{StaticResource Dg}" RowStyle="{StaticResource DgRow}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserResizeRows="False"
                      SelectionMode="Extended" GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      PreviewMouseRightButtonDown="ConnGrid_RightClick"
                      PreviewKeyDown="ConnGrid_KeyDown">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Протокол" Binding="{Binding Protocol}" Width="75"/>
                    <DataGridTemplateColumn Header="Процесс" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" ToolTip="{Binding ProcessPath}">
                                    <Image Source="{Binding Icon}" Width="16" Height="16"
                                           Margin="0,0,8,0" SnapsToDevicePixels="True"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                    <TextBlock Text="{Binding ProcessName}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Программа / сервис" Binding="{Binding Description}" Width="180"/>
                    <DataGridTextColumn Header="↓ Приём" Binding="{Binding DownloadRateText}" SortMemberPath="DownloadRate" Width="90"/>
                    <DataGridTextColumn Header="↑ Отдача" Binding="{Binding UploadRateText}" SortMemberPath="UploadRate" Width="90"/>
                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="60"/>
                    <DataGridTextColumn Header="Локальный адрес" Binding="{Binding LocalEndpoint}" Width="160"/>
                    <DataGridTextColumn Header="Удалённый адрес" Binding="{Binding RemoteEndpoint}" Width="160"/>
                    <DataGridTextColumn Header="Хост (DNS)" Binding="{Binding RemoteHost}" Width="150"/>
                    <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="140"/>
                    <DataGridTextColumn Header="Подпись" Binding="{Binding Signature}" Width="100"/>
                    <DataGridTextColumn Header="Состояние" Binding="{Binding State}" Width="*"/>
                </DataGrid.Columns>

                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="🔌  Закрыть соединение" Click="CloseConn_Click"/>
                        <MenuItem Header="✖  Завершить процесс" Click="Kill_Click"/>
                        <MenuItem Header="⏸  Приостановить процесс" Click="Suspend_Click"/>
                        <MenuItem Header="▶  Возобновить процесс" Click="Resume_Click"/>
                        <Separator/>
                        <MenuItem Header="Приоритет процесса">
                            <MenuItem Header="Низкий (Idle)"          Tag="Idle"        Click="Priority_Click"/>
                            <MenuItem Header="Ниже среднего"          Tag="BelowNormal" Click="Priority_Click"/>
                            <MenuItem Header="Обычный"                Tag="Normal"      Click="Priority_Click"/>
                            <MenuItem Header="Выше среднего"          Tag="AboveNormal" Click="Priority_Click"/>
                            <MenuItem Header="Высокий"                Tag="High"        Click="Priority_Click"/>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="🛡  Заблокировать в брандмауэре" Click="Block_Click"/>
                        <MenuItem Header="🛡  Разблокировать в брандмауэре" Click="Unblock_Click"/>
                        <Separator/>
                        <MenuItem Header="🌐  Определить хост (обратный DNS)" Click="Resolve_Click"/>
                        <MenuItem Header="🌍  Определить страну (GeoIP)" Click="GeoIp_Click"/>
                        <MenuItem Header="⭐  Добавить процесс в доверенные" Click="Trust_Click"/>
                        <MenuItem Header="☆  Убрать из доверенных" Click="Untrust_Click"/>
                        <MenuItem Header="📁  Открыть расположение файла" Click="OpenLocation_Click"/>
                        <MenuItem Header="📋  Копировать строку" Click="Copy_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.ContainerStyle>
                            <Style TargetType="{x:Type GroupItem}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type GroupItem}">
                                            <Expander IsExpanded="False" Margin="0,1"
                                                      Background="{DynamicResource PanelBrush2}"
                                                      Foreground="{DynamicResource FgBrush}"
                                                      BorderBrush="{DynamicResource BorderBrush2}"
                                                      BorderThickness="0,0,0,1">
                                                <Expander.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                        <TextBlock Text="{Binding ItemCount, StringFormat=' — соединений: {0}'}"
                                                                   Foreground="{DynamicResource MutedBrush}"/>
                                                    </StackPanel>
                                                </Expander.Header>
                                                <ItemsPresenter/>
                                            </Expander>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GroupStyle.ContainerStyle>
                    </GroupStyle>
                </DataGrid.GroupStyle>
            </DataGrid>

            <DockPanel Grid.Row="2" Margin="2,8,2,0" LastChildFill="False">
                <TextBlock x:Name="StatusText" Text="Готово" Foreground="{DynamicResource MutedBrush}"/>
                <TextBlock x:Name="CountText" DockPanel.Dock="Right" Foreground="{DynamicResource MutedBrush}"/>
            </DockPanel>
        </Grid>

        <!-- ============ WINDOWS LOG VIEW ============ -->
        <Grid x:Name="LogView" Grid.Row="1" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PanelBrush}" CornerRadius="10"
                    BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1"
                    Padding="12,10" Margin="0,0,0,12">
                <DockPanel LastChildFill="False">
                    <TextBlock Text="Журнал:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource MutedBrush}"/>
                    <Button x:Name="BtnLogSystem"   Content="Система"      Style="{StaticResource TabBtn}" Tag="active"   Margin="0,0,6,0" Click="LogSource_Click"/>
                    <Button x:Name="BtnLogApp"      Content="Приложения"   Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,6,0" Click="LogSource_Click"/>
                    <Button x:Name="BtnLogSecurity" Content="Безопасность" Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,16,0" Click="LogSource_Click"/>

                    <CheckBox x:Name="LogOnlyErrors" Content="Только ошибки и предупреждения" IsChecked="True" VerticalAlignment="Center" Click="LogOnlyErrors_Changed"/>

                    <Button Content="Обновить" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource Btn}" Click="RefreshLog_Click"/>
                    <TextBox x:Name="LogSearch" Width="220" DockPanel.Dock="Right" Style="{StaticResource SearchBox}" TextChanged="LogSearch_TextChanged"/>
                    <TextBlock Text="Поиск:" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="16,0,6,0" Foreground="{DynamicResource MutedBrush}"/>
                </DockPanel>
            </Border>

            <DataGrid x:Name="LogGrid" Grid.Row="1"
                      Style="{StaticResource Dg}" RowStyle="{StaticResource LogRow}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserResizeRows="False"
                      SelectionMode="Single" GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата и время" Binding="{Binding TimeText}" Width="160"/>
                    <DataGridTemplateColumn Header="Уровень" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Level}" FontWeight="SemiBold" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource FgBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Ошибка">
                                                    <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Предупреждение">
                                                    <Setter Property="Foreground" Value="#FFE0B44C"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Источник" Binding="{Binding Source}" Width="200"/>
                    <DataGridTextColumn Header="ID" Binding="{Binding EventId}" Width="70"/>
                    <DataGridTextColumn Header="Сообщение" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <DockPanel Grid.Row="2" Margin="2,8,2,0" LastChildFill="False">
                <TextBlock x:Name="LogStatus" Text="Откройте вкладку, чтобы загрузить журнал." Foreground="{DynamicResource MutedBrush}"/>
            </DockPanel>
        </Grid>

        <!-- ============ SETTINGS SLIDE-OUT PANEL ============ -->
        <Grid x:Name="SettingsOverlay" Grid.Row="0" Grid.RowSpan="2" Visibility="Collapsed" Panel.ZIndex="50">
            <Border Background="#99000000" MouseLeftButtonDown="SettingsScrim_Click"/>
            <Border Width="340" HorizontalAlignment="Left"
                    Background="{DynamicResource BgBrush}"
                    BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="0,0,1,0">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="SettingsSlide" X="-340"/>
                </Border.RenderTransform>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="18">
                        <DockPanel Margin="0,0,0,18" LastChildFill="False">
                            <TextBlock Text="⚙ Настройки" FontSize="18" FontWeight="Bold" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                            <Button Content="✕" DockPanel.Dock="Right" Style="{StaticResource TabBtn}" Click="CloseSettings_Click"/>
                        </DockPanel>

                        <TextBlock Text="Тема оформления" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,22">
                            <Button x:Name="BtnDark"  Content="🌙 Тёмная"  Style="{StaticResource TabBtn}" Tag="active"   Click="Dark_Click"/>
                            <Button x:Name="BtnLight" Content="☀ Светлая"  Style="{StaticResource TabBtn}" Tag="inactive" Margin="8,0,0,0" Click="Light_Click"/>
                        </StackPanel>

                        <TextBlock Text="Таймер автоперезагрузки" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}" Margin="0,0,0,8"
                                   Text="После действий, требующих перезагрузки (сброс Winsock/TCP-IP). При «0 сек» перезагрузка не запускается сама — программа спросит с кнопками «Перезагрузить» и «Отмена»."/>
                        <WrapPanel x:Name="TimerPanel" Margin="0,0,0,22">
                            <Button Content="5 мин" CommandParameter="300" Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,6,6" Click="Timer_Click"/>
                            <Button Content="4 мин" CommandParameter="240" Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,6,6" Click="Timer_Click"/>
                            <Button Content="3 мин" CommandParameter="180" Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,6,6" Click="Timer_Click"/>
                            <Button Content="2 мин" CommandParameter="120" Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,6,6" Click="Timer_Click"/>
                            <Button Content="1 мин" CommandParameter="60"  Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,6,6" Click="Timer_Click"/>
                            <Button Content="0 сек" CommandParameter="0"   Style="{StaticResource TabBtn}" Tag="inactive" Margin="0,0,6,6" Click="Timer_Click"/>
                        </WrapPanel>

                        <TextBlock Text="Обновление программы" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock x:Name="UpdateVersionText" Foreground="{DynamicResource MutedBrush}"
                                   TextWrapping="Wrap" Margin="0,0,0,8" Text="Установленная версия: —"/>
                        <Button x:Name="BtnUpdateApp" Content="🔄 Обновить приложение"
                                HorizontalAlignment="Left" Style="{StaticResource Btn}"
                                Margin="0,0,0,8" Click="UpdateApp_Click"/>
                        <ProgressBar x:Name="UpdateProgress" Height="6" Minimum="0" Maximum="1"
                                     Visibility="Collapsed" Margin="0,0,0,8"/>
                        <TextBlock x:Name="UpdateStatusText" TextWrapping="Wrap"
                                   Foreground="{DynamicResource MutedBrush}" Margin="0,0,0,22"
                                   Text="Проверяет релизы на GitHub, скачивает новую версию, сверяет её контрольную сумму SHA-256 и заменяет старый файл, удаляя ненужное."/>

                        <Border Background="{DynamicResource PanelBrush}" CornerRadius="10"
                                BorderBrush="{DynamicResource DangerBrush}" BorderThickness="1" Padding="14">
                            <StackPanel>
                                <TextBlock Text="Удаление программы" FontWeight="SemiBold" Foreground="{DynamicResource DangerBrush}" Margin="0,0,0,6"/>
                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}" Margin="0,0,0,10"
                                           Text="Полностью удалит NetOptimizer с компьютера: файл программы, вспомогательные файлы и настройки. Необратимо."/>
                                <Button x:Name="BtnUninstall" Content="🗑 Удалить программу"
                                        HorizontalAlignment="Left" Style="{StaticResource Btn}" Click="Uninstall_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
